# Architecture

## 原則

- 関心ごとを分離する
- ファイル責務を明確にする
- 変更点が追跡しやすい構造を保つ

## 推奨

- テーマ: テンプレートとロジックを分離
- プラグイン: 機能単位でディレクトリ分割
- Block 開発: `block.json` を基準に設計

## 禁止

- テンプレートに複雑ロジックを埋め込む
- 単一ファイルへ機能を集中させる
